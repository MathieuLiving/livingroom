// PAGE DÉSACTIVÉE - ANCIENNE LANDING QUARTIER COURBEVOIE
// Conservée uniquement parce que l'hébergeur ne permet pas la suppression.
// Les pages actives sont désormais gérées ailleurs (App.jsx).

import React, { useEffect } from "react";

const CourbevoieQuartierPage = () => {
  useEffect(() => {
    console.warn(
      "⚠️ CourbevoieQuartierPage est désactivée et ne doit plus être utilisée."
    );
  }, []);

  return null; // Aucun rendu
};

export default CourbevoieQuartierPage;